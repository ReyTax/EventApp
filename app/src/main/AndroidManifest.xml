<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="reytax.project.eventapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/EventApp">
        <activity android:name=".menuscreen.event.create.CreateEventActivity"></activity>
        <activity android:name=".menuscreen.profile.UserSettingsActivity" />
        <activity android:name=".menuscreen.profile.ProfileActivity" />
        <activity android:name=".menuscreen.navigation.NavigationBarActivity" />
        <activity android:name=".menuscreen.MenuScreenActivity" />
        <activity android:name=".authentication.ResetPasswordActivity" />
        <activity android:name=".authentication.RegisterActivity" />
        <activity android:name=".authentication.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>